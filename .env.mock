# Loaded only by `npm run dev:mock` (vite --mode mock).
#
# Empty on purpose. An empty base URL keeps API calls relative, so they are
# answered by the mock inside the dev server and nothing reaches a real backend.
# Putting a URL here would send the app to that server and defeat the point.
VITE_API_BASE_URL=
