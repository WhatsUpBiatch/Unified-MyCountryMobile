# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
node_modules
dist
dist-ssr
*.local
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
package-lock.json
.env
# Compiled Python bytecode. Two .pyc files under backend-patches/ were tracked
# by accident, so every run of those services dirtied the working tree with an
# unreadable binary diff. The sources next to them are what matters.
__pycache__/
*.py[cod]

# Encrypted credential archives — must never reach a remote
secrets-backup/

# Live-managed by Claude Code; accumulates local command text incl. credentials
.claude/settings.json

# Timestamped backups left behind by in-place patch scripts (*.bak-20260902-...)
*.bak
*.bak-*
*.orig
*.rej
*~

# Generated lint report
eslint-output.json

# Per-developer permission rules; machine-specific like settings.json above
.claude/settings.local.json

# Sandbox role selection, per-developer
mock/.role

.vercel
.env*
