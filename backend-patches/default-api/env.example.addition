# Appended to default-api's .env (or wherever env.example is deployed from).
# Unset or anything other than the literal string "true" means the IP
# allowlist is not enforced for ANY company, whatever any individual company
# has saved on Company > Security. See README.md's "Rollout" section before
# setting this to true.
IP_ALLOWLIST_ENFORCEMENT_ENABLED=false
