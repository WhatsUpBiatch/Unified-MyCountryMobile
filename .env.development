# Non-secret defaults so a fresh checkout boots without hunting for config.
# Only read when /etc/mycountrymobile-web is absent (see vite.config.ts).
# Every VITE_* value is compiled into the browser bundle and is therefore
# public by definition — never put a real secret in this file.
VITE_API_BASE_URL=https://api2.mycountrymobile.com
