# SANDBOX COPY — do not copy this file back into the real project.
#
# Deliberately blank. The app then requests /api/... from this dev server
# rather than api2 directly, so every call passes through the proxy in
# vite.config.ts, where writes are refused. Setting a real URL here would
# point the browser straight at production and defeat the guard.
VITE_API_BASE_URL=
